#include <iostream>
#include <vector>


int main(){
    int T;
    std::cin >> T;
    for(int i=0; i<T; i++){
        int N;
        int M;
        std::cin >> N >> M;
        std::vector<std::vector<int>> arr(N, std::vector<int>(M));
        std::vector<std::vector<bool>> arr(N, std::vector<bool>(M));
        

    }
    

}